using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class adjlk : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }
    //打开
    // Update is called once per frame
    void Update()
    {
        
    }
}
